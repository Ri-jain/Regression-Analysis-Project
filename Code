import pandas as pd
import numpy as np
from scipy.stats import skew
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import r2_score, mean_absolute_error, mean_squared_error
import statsmodels.api as sm
from statsmodels.stats.anova import anova_lm
import warnings
warnings.filterwarnings('ignore')

customers = pd.read_excel("/Users/filippotazzi/Documents/D'Amore-McKim/Fall 2024/Foundations of Business Analytics/Analytics Problem Set/customers.xlsx", 
                          sheet_name="customers")

def print_unique_values(customers):
    for x in customers.columns:
        print(f"Column: {x}")
        print(customers[x].unique())
        print()

print_unique_values(customers)

state_corrections = {
    "Massachusetts": "MA", "Mass": "MA", "Mass.": "MA", "Massachusets": "MA", "Massachusetts ": "MA",
    "Connecticut": "CT", "Conn.": "CT"
}

customers['customer.state'] = customers['customer.state'].replace(state_corrections)

valid_states = ['AL', 'AK', 'AZ', 'AR', 'CA', 'CO', 'CT', 'DE', 'FL', 'GA', 'HI', 'ID', 'IL', 'IN', 'IA', 'KS', 'KY', 
                'LA', 'ME', 'MD', 'MA', 'MI', 'MN', 'MS', 'MO', 'MT', 'NE', 'NV', 'NH', 'NJ', 'NM', 'NY', 'NC', 'ND', 
                'OH', 'OK', 'OR', 'PA', 'RI', 'SC', 'SD', 'TN', 'TX', 'UT', 'VT', 'VA', 'WA', 'WV', 'WI', 'WY']

customers['customer.state'] = customers['customer.state'].apply(lambda x: x if x in valid_states else np.nan)

month_corrections = {
    "January": 1, "February": 2, "Feb.": 2, "March": 3, "Mar": 3,
    "April": 4, "Apr.": 4, "May": 5, "June": 6, "July": 7,
    "August": 8, "September": 9, "October": 10, "Oct": 10, "Nov.": 11, "November": 11,
    "December": 12
}

customers['birthday.month'] = customers['birthday.month'].replace(month_corrections)

customers['birthday.month'] = pd.to_numeric(customers['birthday.month'], errors='coerce')
customers['birthday.month'] = customers['birthday.month'].apply(lambda x: x if (x >= 1 and x <= 12) else np.nan)

customers = customers.dropna(subset=['birthday.month'])

print_unique_values(customers)

customers['in.store.exp_numeric'] = customers['in.store.exp'].str.extract(r'^(\d+)').astype(float)
customers['in.store.exp_text'] = customers['in.store.exp'].str.replace(r'^\d+', '', regex=True).str.strip()
customers['selection_numeric'] = customers['selection'].str.extract(r'^(\d+)').astype(float)
customers['selection_text'] = customers['selection'].str.replace(r'^\d+', '', regex=True).str.strip()

customers['in.store.exp_numeric'] = customers['in.store.exp_numeric'].fillna(0)
customers['selection_numeric'] = customers['selection_numeric'].fillna(0)

customers = customers.drop(columns=['in.store.exp', 'selection'])

summary_data = {
    'unique_states_after_cleaning': customers['customer.state'].unique(),
    'unique_birthday_months_after_cleaning': customers['birthday.month'].unique(),
    'unique_in_store_exp_numeric': customers['in.store.exp_numeric'].unique(),
    'unique_in_store_exp_text': customers['in.store.exp_text'].unique(),
    'unique_selection_numeric': customers['selection_numeric'].unique(),
    'unique_selection_text': customers['selection_text'].unique(),
    'missing_values_summary': customers.isnull().sum()
}

print(summary_data)

sales = pd.read_excel("/Users/filippotazzi/Documents/D'Amore-McKim/Fall 2024/Foundations of Business Analytics/Analytics Problem Set/sales.xlsx", 
                      sheet_name="sales-data")

customer_summary = sales.groupby('customer.id').agg(
    total_items_purchased=('qty', 'sum'),
    avg_item_sale_price=lambda x: (sales.loc[x.index, 'sale.amount'] / sales.loc[x.index, 'qty']).mean()
).reset_index()

customer_purchases = customer_summary.merge(customers, on='customer.id', how='left')

customer_purchases.to_csv("/Users/filippotazzi/Documents/D'Amore-McKim/Fall 2024/Foundations of Business Analytics/Analytics Problem Set/customer_purchases.csv", 
                          index=False)

customer_purchases.to_excel("/Users/filippotazzi/Documents/D'Amore-McKim/Fall 2024/Foundations of Business Analytics/Analytics Problem Set/customer_purchases.xlsx", 
                            index=False)

sale_amount_stats = sales['sale.amount'].agg(['mean', 'median', 'std'])
sale_amount_stats['skewness'] = skew(sales['sale.amount'].dropna())

print(sale_amount_stats)

plt.figure(figsize=(10, 2))
plt.boxplot(sales['sale.amount'].dropna(), vert=False)
plt.title('General Sales Amount')
plt.xlabel('Sales Amount')
plt.yticks([])
plt.tight_layout()
plt.show()

plt.figure(figsize=(10, 6))
sales.boxplot(column='sale.amount', by='category', vert=False, figsize=(10, 6))
plt.suptitle('')
plt.title('Sales Amount by Product Category')
plt.xlabel('Amount')
plt.ylabel('')
plt.tight_layout()
plt.show()

blended_gross_margin = sales.groupby('category').agg(
    total_sale_amount=('sale.amount', 'sum'),
    total_ext_cost=('ext.cost', 'sum')
).reset_index()
blended_gross_margin['blended_gross_margin'] = (blended_gross_margin['total_sale_amount'] - blended_gross_margin['total_ext_cost']) / blended_gross_margin['total_sale_amount']

print(blended_gross_margin)

Q1 = sales['sale.amount'].quantile(0.25)
Q3 = sales['sale.amount'].quantile(0.75)
IQR = Q3 - Q1

lower_bound = Q1 - 1.5 * IQR
upper_bound = Q3 + 1.5 * IQR

outliers_boxplot = sales[(sales['sale.amount'] < lower_bound) | (sales['sale.amount'] > upper_bound)]

outliers_boxplot.to_excel("/Users/filippotazzi/Documents/D'Amore-McKim/Fall 2024/Foundations of Business Analytics/Analytics Problem Set/outliers_boxplot.xlsx", 
                          index=False)

print(outliers_boxplot)

total_outliers = len(outliers_boxplot)

print(f"Total number of outliers: {total_outliers}")

threshold_value_Mens = 160
threshold_value_Gifts = 80
threshold_value_Foot = 250

sales = sales[~((sales['category'] == "Men's Apparel") & (sales['sale.amount'] > threshold_value_Mens))]
sales = sales[~((sales['category'] == "Gifts & Lifestyle") & (sales['sale.amount'] > threshold_value_Gifts))]
sales = sales[~((sales['category'] == "Footwear") & (sales['sale.amount'] > threshold_value_Foot))]

plt.figure(figsize=(10, 2))
plt.boxplot(sales['sale.amount'].dropna(), vert=False)
plt.title('General Sales Amount')
plt.xlabel('Sales Amount')
plt.yticks([])
plt.tight_layout()
plt.show()

plt.figure(figsize=(10, 6))
sales.boxplot(column='sale.amount', by='category', vert=False, figsize=(10, 6))
plt.suptitle('')
plt.title('Sales Amount by Product Category')
plt.xlabel('Amount')
plt.ylabel('')
plt.tight_layout()
plt.show()

sales['price.category'] = sales['price.category'].astype('category')
sales['category'] = sales['category'].astype('category')
sales['store'] = sales['store'].astype('category')
sales['month'] = pd.to_datetime(sales['sale.date']).dt.month

sales['log_sale_amount'] = np.log(sales['sale.amount'])
sales['log_ext_cost'] = np.log(sales['ext.cost'])

sales['interaction_log'] = sales['log_sale_amount'] * sales['log_ext_cost']
sales['interaction_nolog'] = sales['sale.amount'] * sales['ext.cost']
sales['loyalty_qty_interaction'] = sales['loyalty.member'] * sales['qty']

np.random.seed(123)
train_data, test_data = train_test_split(sales, test_size=0.2, random_state=123)

X_train = pd.get_dummies(train_data[['price.category', 'category', 'store', 'month', 'qty', 'unit.cost', 'sale.amount']], drop_first=True)
y_train = train_data['gross.margin']
X_train_sm = sm.add_constant(X_train)

model = sm.OLS(y_train, X_train_sm).fit()

print(model.summary())

train_data_interactions = train_data.copy()
train_data_interactions['qty_squared'] = train_data_interactions['qty'] ** 2
train_data_interactions['unit_cost_squared'] = train_data_interactions['unit.cost'] ** 2
train_data_interactions['sale_amount_squared'] = train_data_interactions['sale.amount'] ** 2

formula_parts = ['store', 'month', 'qty', 'qty_squared', 'unit.cost', 'unit_cost_squared', 'sale.amount', 'sale_amount_squared']
X_train_interactions = pd.get_dummies(train_data_interactions[formula_parts], drop_first=True)

price_cat_dummies = pd.get_dummies(train_data_interactions['price.category'], prefix='price.category', drop_first=True)
category_dummies = pd.get_dummies(train_data_interactions['category'], prefix='category', drop_first=True)

for pc_col in price_cat_dummies.columns:
    for cat_col in category_dummies.columns:
        X_train_interactions[f'{pc_col}:{cat_col}'] = price_cat_dummies[pc_col] * category_dummies[cat_col]

X_train_interactions = pd.concat([X_train_interactions, price_cat_dummies, category_dummies], axis=1)
X_train_interactions_sm = sm.add_constant(X_train_interactions)

model_with_interactions = sm.OLS(y_train, X_train_interactions_sm).fit()

print(model_with_interactions.summary())

sales = pd.get_dummies(sales, columns=['month'], prefix='month', drop_first=True)

train_data, test_data = train_test_split(sales, test_size=0.2, random_state=123)

month_cols = [col for col in train_data.columns if col.startswith('month_')]
formula_vars = ['store', 'qty', 'unit.cost', 'unit_cost_squared', 'sale.amount', 'sale_amount_squared'] + month_cols

train_data['unit_cost_squared'] = train_data['unit.cost'] ** 2
train_data['sale_amount_squared'] = train_data['sale.amount'] ** 2

X_train_dummies = pd.get_dummies(train_data[formula_vars], drop_first=True)
y_train = train_data['gross.margin']
X_train_dummies_sm = sm.add_constant(X_train_dummies)

model_with_dummies = sm.OLS(y_train, X_train_dummies_sm).fit()

print(model_with_dummies.summary())

other_vars = ['store', 'qty', 'unit.cost', 'unit.original.retail', 'sale.amount', 'loyalty.member'] + month_cols
X_train_other = pd.get_dummies(train_data[other_vars], drop_first=True)
X_train_other_sm = sm.add_constant(X_train_other)

other_model = sm.OLS(y_train, X_train_other_sm).fit()

print(other_model.summary())

intr_vars = ['store', 'loyalty_qty_interaction', 'unit.cost', 'unit.original.retail', 'interaction_log'] + month_cols
X_train_intr = pd.get_dummies(train_data[intr_vars], drop_first=True)
X_train_intr_sm = sm.add_constant(X_train_intr)

intr_model = sm.OLS(y_train, X_train_intr_sm).fit()

print(intr_model.summary())

log_vars = ['store', 'qty', 'unit.cost', 'unit.original.retail', 'interaction_log'] + month_cols
X_train_log = pd.get_dummies(train_data[log_vars], drop_first=True)
X_train_log_sm = sm.add_constant(X_train_log)

log_model = sm.OLS(y_train, X_train_log_sm).fit()

print(log_model.summary())

nolog_vars = ['store', 'qty', 'unit.cost', 'unit.original.retail', 'interaction_nolog'] + month_cols
X_train_nolog = pd.get_dummies(train_data[nolog_vars], drop_first=True)
X_train_nolog_sm = sm.add_constant(X_train_nolog)

nolog_model = sm.OLS(y_train, X_train_nolog_sm).fit()

print(nolog_model.summary())

log_vars = ['store', 'qty', 'unit.cost', 'unit.original.retail', 'interaction_log'] + month_cols
X_train_log = pd.get_dummies(train_data[log_vars], drop_first=True)
X_train_log_sm = sm.add_constant(X_train_log)

log_model = sm.OLS(y_train, X_train_log_sm).fit()

print(log_model.summary())

test_data['unit_cost_squared'] = test_data['unit.cost'] ** 2
test_data['sale_amount_squared'] = test_data['sale.amount'] ** 2

X_test_interactions = test_data[X_train_interactions.columns.difference(['const'])]
X_test_interactions_sm = sm.add_constant(X_test_interactions, has_constant='add')

predictions_interactions = model_with_interactions.predict(X_test_interactions_sm)

r_squared_interactions = r2_score(test_data['gross.margin'], predictions_interactions)
mae_interactions = mean_absolute_error(test_data['gross.margin'], predictions_interactions)

print(f"Test R-squared with Interactions: {r_squared_interactions}")
print(f"Mean Absolute Error (MAE) with Interactions: {mae_interactions}")

print(anova_lm(model, model_with_interactions))

X_test_dummies = test_data[X_train_dummies.columns.difference(['const'])]
X_test_dummies_sm = sm.add_constant(X_test_dummies, has_constant='add')

predictions_dummies = model_with_dummies.predict(X_test_dummies_sm)

r_squared_dummies = r2_score(test_data['gross.margin'], predictions_dummies)
mae_dummies = mean_absolute_error(test_data['gross.margin'], predictions_dummies)

print(f"Test R-squared with Dummies: {r_squared_dummies}")
print(f"Mean Absolute Error (MAE) with Dummies: {mae_dummies}")

mse = mean_squared_error(test_data['gross.margin'], predictions_dummies)
mad = mean_absolute_error(test_data['gross.margin'], predictions_dummies)
mape = np.mean(np.abs((predictions_dummies - test_data['gross.margin']) / test_data['gross.margin'])) * 100
rmse = np.sqrt(mse)

print("Performance Metrics:")
print(f"Mean Square Error (MSE): {mse}")
print(f"Mean Absolute Deviation (MAD): {mad}")
print(f"Mean Absolute Percentage Error (MAPE): {mape}%")
print(f"Root Mean Square Error (RMSE): {rmse}")

print(anova_lm(model_with_interactions, model_with_dummies))

month_coefficients = {k: v for k, v in model_with_dummies.params.items() if k.startswith('month_')}

month_names = ["February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"]
month_impacts = [month_coefficients.get(f'month_{i}', 0) for i in range(2, 13)]

month_impact = pd.DataFrame({
    'Month': ['January'] + month_names,
    'Impact': [0] + month_impacts
})

month_impact['Month'] = pd.Categorical(month_impact['Month'], 
                                       categories=["January", "February", "March", "April", "May", 
                                                   "June", "July", "August", "September", "October", 
                                                   "November", "December"], 
                                       ordered=True)

plt.figure(figsize=(10, 6))
colors = plt.cm.RdYlGn((month_impact['Impact'] - month_impact['Impact'].min()) / (month_impact['Impact'].max() - month_impact['Impact'].min()))
plt.barh(month_impact['Month'], month_impact['Impact'], color=colors, edgecolor='black')
plt.xlabel('')
plt.ylabel('')
plt.title('Monthly Impact on Gross Margins (Relative to January)', fontsize=16)
plt.gca().invert_yaxis()
plt.tight_layout()
plt.show()

X_test = test_data[X_train.columns.difference(['const'])]
X_test_sm = sm.add_constant(X_test, has_constant='add')

predictions = model.predict(X_test_sm)

r_squared = r2_score(test_data['gross.margin'], predictions)
mae = mean_absolute_error(test_data['gross.margin'], predictions)

print(f"Test R-squared: {r_squared}")
print(f"Mean Absolute Error (MAE): {mae}")

print("\nRegression Coefficients:")
print(model.params)
